/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  images:{
    domains:["upload.wikimedia.org","cdn.shopify.com","freepngimg.com"]
  }
}

module.exports = nextConfig
